<div class="main posts-show">
  <div class="container">
    <div class="posts-show-item">
      <p>
        <!-- Display the @post's content -->
        <%= @post.content %>
      </p>
      <!-- Paste the HTML here -->
      <div class="post-time">
        <!-- Display the @post's created_at -->
        <%= @post.created_at %>
      </div>
      <!-- Add a link that redirects to the Edit post page -->
      <div class="post-menus">
        <%= link_to("Edit", "/posts/#{@post.id}/edit") %>
        <%= link_to("Delete", "/posts/#{@post.id}/destroy", {method: "post"}) %>
      </div>
    </div>
  </div>
</div>
